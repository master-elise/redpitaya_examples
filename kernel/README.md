* ``dev``: using the legacy /dev "all is a file" unix philosophy, including interrupt management
* ``sys``: demonstration of the ``/sys`` filesystem (device/driver)
* ``iio``: layer on top of ``sys`` for ADi's Industrial IO (IIO) framework
